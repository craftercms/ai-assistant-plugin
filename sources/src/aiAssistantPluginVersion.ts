/**
 * Plugin release version for client-side features (e.g. configuration joyride).
 * Keep in sync with `craftercms-plugin.yaml` → `plugin.version`.
 */
export const AI_ASSISTANT_PLUGIN_VERSION = '1.0.0';
