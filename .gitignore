.idea
chatgpt-plugin.iml

# Cursor IDE (local rules, skills, agent state — do not commit)
.cursor/

# Dependencies (install with yarn in repo root and/or sources/)
node_modules/

# Local Studio auth (never commit)
scripts/.studio-token
scripts/.studio-token-b
.env.local

# Local integration payloads (copy from create-site.json.example)
scripts/test/integration/create-site.json
# Local chat scenario overrides (copy from scenarios/chat-scenarios.example.json)
scripts/test/scenarios/chat-scenarios.json

# Generated test run output (run-all.sh, run-chat-scenarios.mjs, rest-contracts.sh)
scripts/test/.run-all-report.jsonl
scripts/test/*.report.jsonl
